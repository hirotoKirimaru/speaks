"""議事録要約 / 補正のプロンプトテンプレート。

論文の知見:
- GenSEC (Chen+ 2024, arXiv:2409.09785): LLM による ASR 補正は構造保持と
  N-best 系の制約が品質に効く。本実装では行構造保持を厳格にプロンプトで縛る。
- Action-Item-Driven Summarization (2023, arXiv:2312.17581): 要約タスクを
  分解 (トピック抽出 → 議事録合成) するとローカル LLM でも品質が安定する。
"""

# 既存単段プロンプト (--summary-pipeline single で利用)
SUMMARY_PROMPT = """以下は会議の文字起こしです (話者ラベルなし)。
発言内容から話者を推定し、可能な範囲で発話主を特定しつつ議事録形式にまとめてください。
話者が不明な場合は「発言者不明」で構いません。

## 出力フォーマット（必ずこの形式で）

# 議事録

## トピック
1. （話題を箇条書き）

## 決定事項
- （決まったこと。なければ「特になし」）

## TODO
- [ ] （やるべきこと。なければ「特になし」）

## 議論メモ
- （話の流れや重要な発言を要約）

---

## 文字起こし原文:

{transcript}
"""

# 多段の第 1 段: トピック抽出
TOPIC_PROMPT = """以下は会議の文字起こしです。
話題 (トピック) を最大 5 件、1 件 1 行・1 文以内で簡潔に箇条書きしてください。
余計な前置きや結論は書かないこと。

## 出力フォーマット (Markdown)
- トピック1
- トピック2
- ...

## 文字起こし原文:

{transcript}
"""

# 多段の第 2 段: 議事録合成 (トピック注入版)
MINUTES_PROMPT = """以下は会議の文字起こしと、別途抽出済みのトピック一覧です。
両者を踏まえて議事録を作成してください。発言内容から話者を推定し、可能な範囲で
発話主を特定しつつまとめてください。話者が不明な場合は「発言者不明」で構いません。

## 出力フォーマット（必ずこの形式で）

# 議事録

## トピック
（与えられたトピック一覧をそのまま列挙）

## 決定事項
- （決まったこと。なければ「特になし」）

## TODO
- [ ] （やるべきこと。なければ「特になし」）

## 議論メモ
- （話の流れや重要な発言を、トピックに沿って要約）

---

## 既出のトピック一覧:

{topics}

## 文字起こし原文:

{transcript}
"""

# LLM による ASR 補正プロンプト
CORRECTION_PROMPT = """以下は音声認識 (ASR) の出力です。各行は
`[start.s - end.s] SPEAKER_XX: text` または `[start.s - end.s] text` の形式です。

次のルールに厳密に従い、補正後の transcript だけを出力してください。
余計な前置きや解説、コードブロックの開始記号 (```) は付けないこと。

## ルール
1. 行数を一切増減しないこと。入力 N 行なら出力も同じ N 行。
2. 各行の `[start - end]` タイムスタンプと話者ラベル (`SPEAKER_XX:`) はそのまま残すこと。
3. 内容を創作・追記しないこと。表記揺れ、助詞の修正、同音異義語の選択にのみ留めること。
4. 不確かな単語は元のまま残すこと。確信が無いのに置換しないこと。
5. 与えられた「正しい表記の固有名詞」リストの語が出現する場合は、その表記に揃えること。

## 正しい表記の固有名詞 (vocab):

{vocab}

## ASR 出力:

{transcript}
"""
